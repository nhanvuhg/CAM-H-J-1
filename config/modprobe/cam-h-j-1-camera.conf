# V3Link receivers are not always ready when udev probes camera sensors during
# early boot.  An early IMX477 I2C failure leaks cam_reset_gpio in NVIDIA's
# driver until the next reboot.  Suppress alias-based auto-loading; start_all.sh
# explicitly loads nv_imx477 after the desktop and external camera power settle.
blacklist nv_imx477
